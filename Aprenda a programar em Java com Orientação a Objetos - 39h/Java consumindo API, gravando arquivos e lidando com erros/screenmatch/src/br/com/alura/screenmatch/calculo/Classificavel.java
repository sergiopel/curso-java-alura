package br.com.alura.screenmatch.calculo;

// a interface diz que toda classe que a implementa (que assina o "contrato"), precisa obrigatoriamente
// também implementarer o método dessa interface
public interface Classificavel {
    int getClassificacao();
}
